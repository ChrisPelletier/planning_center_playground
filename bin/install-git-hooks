#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel)"
cd "$ROOT_DIR"

git config core.hooksPath .githooks

echo "Git hooks path set to .githooks"
echo "If you want this for all clones, run: git config --local core.hooksPath .githooks"
